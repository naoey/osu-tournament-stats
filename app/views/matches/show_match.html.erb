<%= content_for :title, @match.round_name %>
<%= react_component('matches/MatchDetails', {
    match: @match,
    tableProps: {
      hiddenColumns: %w[matches_played matches_won matches_won_percent]
    }
}) %>
