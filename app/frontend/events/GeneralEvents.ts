export enum GeneralEvents {
  TournamentCreated = "tournaments.created",
  TournamentDeleted = "tournaments.deleted",
  TournamentUpdated = "tournaments.updated",
  MatchCreated = "matches.created",
  MatchDeleted = "matches.deleted",
  MatchUpdated = "matches.updated",
}
